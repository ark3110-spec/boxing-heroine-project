// Player-facing release version. Change this when shipping a new game release.
export const GAME_VERSION = "1.1.0";

// Campaign snapshot format, independent of game releases. Keep at 1 until an
// explicit save migration is implemented; existing version-1 saves still load.
export const SAVE_VERSION = 1;
